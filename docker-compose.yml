name: taama-ccc-infra

services:
  qdrant:
    container_name: qdrant
    restart: unless-stopped
    image: qdrant/qdrant
    ports:
      - 6333:6333
    volumes:
      - ./docker/volumes/qdrant:/qdrant/storage
